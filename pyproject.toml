[project]
name = "dachser-live-transit-planner"
version = "3.0.0"
description = "DACHSER Live Transit Planner"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115,<1.0",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.9",
    "httpx>=0.27",
    "tzdata>=2024.1",
]

[tool.vercel]
entrypoint = "main:app"
